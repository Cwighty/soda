<?xml version="1.0" encoding="utf-8" ?>
<views:BasePage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                xmlns:views="clr-namespace:CustomerApp.Views"
                xmlns:controls="clr-namespace:CustomerApp.Views.Controls"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                x:Class="CustomerApp.Views.FeaturePage"
                Title="FeaturePage">
    <StackLayout Grid.Row="1"
                 Margin="0,20,0,0">
        <ScrollView VerticalOptions="FillAndExpand">

            <CollectionView ItemsSource="{Binding CategorizedProducts}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <controls:HorizontalProductDisplay 
                            Title="{Binding Name}"
                            Products="{Binding Products}"
                            ShowAllEnabled="False"
                            />
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </ScrollView>
    </StackLayout>
</views:BasePage>